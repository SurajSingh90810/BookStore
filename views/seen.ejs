<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Book List</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body>
  <div class="container mt-5">
    <h2 class="text-center mb-4">Book List</h2>
    <div class="table-responsive">
      <table class="table table-bordered table-striped text-center">
        <thead class="table-dark">
          <tr>
            <th>Book Name</th>
            <th>Author</th>
            <th>Publication Date</th>
            <th>Price</th>
            <th>Description</th>
            <th>Quantity</th>
            <th>Image</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% if (books && books.length> 0) { %> <% books.forEach((book)=> {
              %>
              <tr>
                <td>
                  <%= book.username %>
                </td>
                <td>
                  <%= book.password %>
                </td>
                <td>
                  <%= book.date %>
                </td>
                <td>
                  <%= book.price %>
                </td>
                <td>
                  <%= book.description %>
                </td>
                <td>
                  <%= book.quantity %>
                </td>
                <td>
                  <img src="/<%= book.image %>" alt="Book Image" class="img-thumbnail" width="100" />
                </td>
                <td>
                  <a href="/edit/<%= book._id %>" class="btn btn-warning btn-sm">Edit</a>
                  <a href="/delete/<%= book._id %>" class="btn btn-danger btn-sm">Delete</a>
                </td>
              </tr>
              <% }) %>
                <% } else { %>
                  <tr>
                    <td colspan="8" class="text-center">No books found.</td>
                  </tr>
                  <% } %>
        </tbody>
      </table>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>